# Example configuration for real integration tests
# Copy this to your .env and fill in your values

# ============================================
# REAL TEST CONFIGURATION
# ============================================

# Enable real tests (set to 1 to run real Discord API tests)
ENABLE_REAL_TESTS=0

# Your Discord user token (required for real tests)
# Get it from Discord web app (DevTools → Network → authorization header)
DISCORD_TOKEN=your_discord_token_here

# Test server ID (use a TEST server, not production!)
# Right-click server → Copy ID (need Developer Mode enabled)
TEST_SERVER_ID=your_test_server_id_here

# Optional: Specific test channel ID (if not set, tests all channels)
TEST_CHANNEL_ID=your_test_channel_id_here

# ============================================
# TEST BEHAVIOR CONFIGURATION
# ============================================

# Maximum messages to import in tests (default: 100)
TEST_MAX_MESSAGES=100

# How many hours back to import messages (default: 1)
TEST_HOURS_BACK=1

# Seconds to wait between API calls (default: 1.0)
# Increase if hitting rate limits
TEST_RATE_LIMIT_SLEEP=1.0

# Automatically delete test database after tests (default: 0)
# Set to 1 to clean up, or 0 to keep for inspection
CLEANUP_TEST_DB=0

# ============================================
# PRODUCTION CONFIGURATION
# ============================================
# These are your regular production settings

# OpenAI API key for LLM features
OPENAI_API_KEY=your_openai_key_here

# Google Gemini API key for summarization
GOOGLE_API_KEY=your_google_api_key_here

# OpenRouter API key (optional)
OPENROUTER_API_KEY=your_openrouter_key_here

# ============================================
# NOTES
# ============================================
# 
# To run real tests:
# 1. Set ENABLE_REAL_TESTS=1
# 2. Fill in DISCORD_TOKEN
# 3. Fill in TEST_SERVER_ID (use a test server!)
# 4. Run: pytest -m real -v -s
#
# Real tests will:
# - Connect to actual Discord API
# - Import real messages
# - Store in data/test_real_db.sqlite (separate from production)
# - Verify data integrity
# - Test incremental imports
#
# See tests/real/README.md for full documentation
